{{- if .Values.registrationService.enabled }}

apiVersion: apps/v1
kind: Deployment

metadata:
  name: registration-service
  labels:
    app: registration-service

spec:
  replicas: {{ .Values.registrationService.replicas }}

  strategy:
    type: RollingUpdate

    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1

  selector:
    matchLabels:
      app: registration-service

  template:
    metadata:
      labels:
        app: registration-service

    spec:
      containers:
        - name: registration-service

          image: "{{ .Values.registrationService.image.repository }}:{{ .Values.registrationService.image.tag }}"

          imagePullPolicy: {{ .Values.global.imagePullPolicy }}

          ports:
            - name: http
              containerPort: {{ .Values.registrationService.port }}
              protocol: TCP

          envFrom:
            - configMapRef:
                name: platform-config

            - secretRef:
                name: platform-secrets

          readinessProbe:
            httpGet:
              path: {{ .Values.registrationService.readinessPath }}
              port: http

            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 6

          livenessProbe:
            httpGet:
              path: {{ .Values.registrationService.livenessPath }}
              port: http

            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 5
            failureThreshold: 3

          resources:
{{ toYaml .Values.registrationService.resources | indent 12 }}

---

apiVersion: v1
kind: Service

metadata:
  name: registration-service
  labels:
    app: registration-service

spec:
  type: ClusterIP

  selector:
    app: registration-service

  ports:
    - name: http
      port: {{ .Values.registrationService.port }}
      targetPort: http
      protocol: TCP

{{- end }}